import SwiftUI

struct LoginView: View {
    var body: some View {
        VStack {
            Text("Login")
                .font(.largeTitle.bold())
            Text("Coming soon")
                .foregroundStyle(.secondary)
        }
    }
}

#Preview { LoginView() }
