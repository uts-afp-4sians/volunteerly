import SwiftUI

struct SignupFormView: View {
    var body: some View {
        VStack {
            Text("Create Profile")
                .font(.largeTitle.bold())
            Text("Coming soon")
                .foregroundStyle(.secondary)
        }
    }
}

#Preview { SignupFormView() }
