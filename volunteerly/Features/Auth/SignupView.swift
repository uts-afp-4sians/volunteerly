import SwiftUI

struct SignupView: View {
    var body: some View {
        VStack {
            Text("Sign Up")
                .font(.largeTitle.bold())
            Text("Coming soon")
                .foregroundStyle(.secondary)
        }
    }
}

#Preview { SignupView() }
